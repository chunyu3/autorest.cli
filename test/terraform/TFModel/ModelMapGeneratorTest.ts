import { expect } from 'chai';
import 'mocha';
import { ModelMapGenerator } from '../../../src/Terraform/TFModel/ModelMapGenerator';
describe('model test', () => {
    it("pass", ()=>{
        // let generater : ModelMapGenerator = new ModelMapGenerator(nil, "network", nil, nil);
        // generater.transfer();
        let a:string = "a";
        let b:string = "b";
        // expect(a).to.equal(b);
    });
    
});
